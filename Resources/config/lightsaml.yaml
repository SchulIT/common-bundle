services:
  _defaults:
      autowire: true
      public: false

  SchoolIT\CommonBundle\Store\IdStore:
    arguments:
      $timeProvider: "@lightsaml.system.time_provider"

  SchoolIT\CommonBundle\Security\Factory\SamlSpTokenFactory: ~

